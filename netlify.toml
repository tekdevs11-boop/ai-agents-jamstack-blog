# Netlify Build Configuration
# npm install se llama explícitamente para garantizar que node_modules
# esté disponible antes de compilar en el entorno CI de Netlify.

[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
